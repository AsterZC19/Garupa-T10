# ============================================================================
# 榜单数据源配置
# 月榜数据来自 GarupaSpeedTracker 后端（StarFreedomX/GarupaSpeedTracker），
# 该后端已持有官方 garupa API 的设备签名。复制为 backend/.env 后填写。
# ============================================================================

# 三个接口可独立配置，只填写协议和域名（可带端口），不要填写 /api 或接口路径。
# 后端自动拼接 /api/eventtop/data、/api/monthlyRanking/info.json、/api/monthlyRanking/top。
# 未设置或留空时：活动榜单使用 Bestdori，月榜使用下方 GARUPA_TRACKER_BASE。
# 复制为 backend/.env 后，取消需要的配置行注释即可切换；修改后重启后端和 scheduler。
# EVENT_TOP_BASE_URL=https://test.starminus.uk
# MONTHLY_INFO_BASE_URL=https://test.starminus.uk
# MONTHLY_TOP_BASE_URL=https://test.starminus.uk

# 月榜的兼容默认基址（旧配置保留 /api）；上面的独立域名配置优先于此设置。
# 本机 docker compose 部署默认 http://127.0.0.1:5519/api；远程部署填完整地址。
# GARUPA_TRACKER_BASE=http://127.0.0.1:5519/api

# tracker 后端服务器索引（0=日服，1=国际服，2=台服，3=国服）
# GARUPA_TRACKER_SERVER=0

# tracker 请求超时（秒）
# GARUPA_TRACKER_TIMEOUT=30
